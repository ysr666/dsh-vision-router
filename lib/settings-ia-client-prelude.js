const CLIENT_MARK = 'data-vision-router-settings-ia'

export const SETTINGS_IA_CLIENT_PRELUDE = String.raw`(function(){
  'use strict';
  var TARGET='dsh-vision-router';
  var SECTION_ID='vision-router';
  var contexts=typeof WeakMap==='function'?new WeakMap():undefined;

  function zh(){try{return String(document.documentElement.lang||'').toLowerCase().startsWith('zh');}catch(_){return true;}}
  function tx(cn,en){return zh()?cn:en;}
  function arr(value){return Array.isArray(value)?value:[];}
  function obj(value){return value&&typeof value==='object'&&!Array.isArray(value)?value:{};}
  function same(a,b){try{return JSON.stringify(a)===JSON.stringify(b);}catch(_){return false;}}
  function rpcValue(body){if(body&&typeof body==='object'&&body.result&&typeof body.result==='object')return body.result.ok===true?body.result.value:undefined;return body;}
  function localPage(){try{var host=String(window.location.hostname||'').toLowerCase();return host==='localhost'||host.endsWith('.localhost')||host==='::1'||host==='[::1]'||/^127(?:\.\d{1,3}){3}$/.test(host);}catch(_){return false;}}
  function builtinRow(row){return !!row&&row.provider==='vision-http'&&typeof row.model==='string'&&row.model.indexOf('ovh/')===0;}
  function cloneRow(row){return {provider:String(row&&row.provider||''),model:String(row&&row.model||''),fallbacks:arr(row&&row.fallbacks).slice()};}
  function splitChain(value){var builtins=[],users=[];arr(value).forEach(function(row){if(!row||typeof row!=='object')return;(builtinRow(row)?builtins:users).push(cloneRow(row));});return {builtins:builtins,users:users};}
  function chainUiRows(value){var rows=splitChain(value).users;return rows.length?rows:[{provider:'',model:'',fallbacks:[]}];}
  function canonicalChain(saved,rows){var builtins=splitChain(saved).builtins,users=[];arr(rows).forEach(function(row){var provider=String(row&&row.provider||'').trim(),model=String(row&&row.model||'').trim();if(!provider&&!model)return;users.push({provider:provider,model:model,fallbacks:arr(row&&row.fallbacks).filter(function(x){return typeof x==='string'&&x.trim();})});});if(users.length)return users.concat(builtins);if(builtins.length)return builtins;return [{provider:'vision-http',model:'ovh/Qwen3.5-397B-A17B',fallbacks:[]}];}
  function validChain(rows){return arr(rows).every(function(row){var p=String(row&&row.provider||'').trim(),m=String(row&&row.model||'').trim();return (!p&&!m)||(!!p&&!!m);});}
  function expandWrappers(value){var rows=[];arr(value).forEach(function(entry){if(!entry||typeof entry.provider!=='string'||!entry.provider)return;var models=arr(entry.models);if(models.length===0)rows.push({provider:entry.provider,model:''});else models.forEach(function(model){if(typeof model==='string'&&model)rows.push({provider:entry.provider,model:model});});});return rows.length?rows:[{provider:'',model:''}];}
  function collapseWrappers(rows){var merged=new Map();arr(rows).forEach(function(row){var provider=String(row&&row.provider||'').trim(),model=String(row&&row.model||'').trim();if(!provider)return;if(!merged.has(provider))merged.set(provider,new Set());var set=merged.get(provider);if(set===null)return;if(!model)merged.set(provider,null);else set.add(model);});return Array.from(merged.entries()).map(function(pair){return {provider:pair[0],models:pair[1]===null?[]:Array.from(pair[1])};});}
  function validWrapperRows(rows){return arr(rows).every(function(row){return !row||!row.model||!!row.provider;});}
  function canonicalGuidance(rows){return arr(rows).map(function(row){return {kind:String(row&&row.kind||'').trim(),text:String(row&&row.text||'').trim()};}).filter(function(row){return row.kind&&row.text;});}
  function validGuidance(rows){return arr(rows).every(function(row){var k=String(row&&row.kind||'').trim(),t=String(row&&row.text||'').trim();return (!k&&!t)||(!!k&&!!t);});}
  function modelName(group,model){if(!model)return '';var hit=arr(group&&group.models).find(function(item){return item&&item.id===model;});return hit&&hit.name&&hit.name!==model?hit.name+' ('+model+')':model;}
  function availableGroups(groups,snapshot){var value=snapshot&&snapshot.value&&typeof snapshot.value==='object'?snapshot.value:{};var wrapper=typeof value.wrapperRoute==='string'&&value.wrapperRoute?value.wrapperRoute:'deepseek-vision';var chain=typeof value.chainRoute==='string'&&value.chainRoute?value.chainRoute:'vision-chain';return arr(groups).filter(function(group){if(!group||typeof group.id!=='string'||!group.id)return false;if(group.id==='vision-http'||group.id===wrapper||group.id===chain||group.id==='deepseek-official-native')return false;if(group.id.endsWith('-vision'))return false;return true;});}

  function patchCopy(namespace,dictionaries){
    if(namespace!==SECTION_ID||!dictionaries||typeof dictionaries!=='object')return dictionaries;
    var next=Object.assign({},dictionaries);
    if(next.zh&&typeof next.zh==='object')next.zh=Object.assign({},next.zh,{
      quickStartTitle:'聊天模型和识图模型分开设置',
      quickStartBody:'聊天模型在聊天页选择；需要看图时点击输入框旁的「👁 识图」。这里仅设置负责读取图片的模型。',
      onboardingBody:'第一次使用只需要记住：聊天模型在聊天页选择，需要看图时点击「👁 识图」；这里设置负责读取图片的模型。',
      onboardingStep1Body:'在聊天页选择你平时使用的聊天模型。需要看图时点击输入框旁的「👁 识图」，不用寻找「+ 自动识图」模型。',
      onboardingStep2Body:'打开「设置 → Vision Router → 常规」，选择负责读取图片的模型；也可以直接使用内置免费兜底。',
      onboardingStep3Title:'3 · 备用识图模型（可选）',
      onboardingStep3Body:'只有需要更高可用性时才添加备用模型；前一个失败时会自动尝试下一个，最后还可以使用内置免费兜底。',
      guideStep1Body:'聊天页输入框旁的模型选择器和「👁 识图」已经高亮。选择你平时使用的聊天模型；需要看图时点击「👁 识图」。选好后点击「下一步」。',
      guidePromptNavBody:'在设置面板左侧点击被高亮的「Vision Router」入口（或点「下一步」自动进入），然后停留在「常规」页选择识图模型。',
      guideChainTitle:'最后 · 确认识图模型',
      guideChainBody:'这里的识图模型会从上到下依次尝试。备用模型完全可选；全部失败时仍可使用内置免费兜底。确认后保存即可。'
    });
    if(next.en&&typeof next.en==='object')next.en=Object.assign({},next.en,{
      quickStartTitle:'Choose chat and vision models separately',
      quickStartBody:'Choose your chat model in the composer. When an image needs inspection, use the “👁 Vision” control beside the input. This page only chooses the models that read images.',
      onboardingBody:'For first use, remember: choose the chat model in chat, use “👁 Vision” when an image needs inspection, and choose the image-reading model here.',
      onboardingStep1Body:'Choose the chat model you normally use. When an image needs inspection, use the “👁 Vision” control beside the input; you do not need to look for a “+ Auto Vision” model.',
      onboardingStep2Body:'Open Settings → Vision Router → General and choose the model that reads images, or keep using the built-in free fallback.',
      onboardingStep3Title:'3 · Add fallback vision models (optional)',
      onboardingStep3Body:'Add fallbacks only when you want extra availability. If one fails, Vision Router tries the next and can still finish with the built-in free fallback.',
      guideStep1Body:'The chat-model selector and “👁 Vision” control beside the input are highlighted. Choose your normal chat model and use “👁 Vision” when an image needs inspection. Click Next when ready.',
      guidePromptNavBody:'Click the highlighted Vision Router entry in Settings (or press Next), then stay on General to choose the vision model.',
      guideChainTitle:'Final · Confirm the vision model',
      guideChainBody:'Vision models are tried from top to bottom. Fallback models are optional, and the built-in free fallback remains available at the end. Save when ready.'
    });
    return next;
  }
  function wrapLocale(locale){if(!locale||(typeof locale!=='object'&&typeof locale!=='function'))return locale;return new Proxy(locale,{get:function(target,property){if(property==='define'){var define=Reflect.get(target,property,target);if(typeof define!=='function')return define;return function(namespace,dictionaries){return define.call(target,namespace,patchCopy(namespace,dictionaries));};}var value=Reflect.get(target,property,target);return typeof value==='function'?value.bind(target):value;}});}

  function makeComponent(React){
    return function VisionRouterSettingsIA(props){
      var scope=props&&props.scope;
      var subscribe=React.useMemo(function(){return scope&&typeof scope.subscribe==='function'?scope.subscribe.bind(scope):function(){return function(){};};},[scope]);
      var getSnapshot=React.useMemo(function(){return scope&&typeof scope.getSnapshot==='function'?scope.getSnapshot.bind(scope):function(){return undefined;};},[scope]);
      var snapshot=React.useSyncExternalStore(subscribe,getSnapshot);
      var pagePair=React.useState('general'),page=pagePair[0],setPage=pagePair[1];
      var draftsPair=React.useState({}),drafts=draftsPair[0],setDrafts=draftsPair[1];
      var chainPair=React.useState(undefined),chainDraft=chainPair[0],setChainDraft=chainPair[1];
      var wrapperPair=React.useState(undefined),wrapperDraft=wrapperPair[0],setWrapperDraft=wrapperPair[1];
      var guidancePair=React.useState(undefined),guidanceDraft=guidancePair[0],setGuidanceDraft=guidancePair[1];
      var savePair=React.useState({status:'idle'}),saveState=savePair[0],setSaveState=savePair[1];
      var catalogPair=React.useState({status:'idle',groups:[]}),catalog=catalogPair[0],setCatalog=catalogPair[1];
      var openPair=React.useState({ollama:false,lmstudio:false,developer:false}),open=openPair[0],setOpen=openPair[1];

      function loadCatalog(){
        setCatalog(function(previous){return {status:'loading',groups:previous&&previous.groups?previous.groups:[]};});
        try{
          var connection=props&&typeof props.getConnection==='function'?props.getConnection():undefined;
          var models=connection&&connection.api&&connection.api.llm&&connection.api.llm.models;
          if(typeof models!=='function'){setCatalog({status:'error',groups:[]});return Promise.resolve();}
          return Promise.resolve(models.call(connection.api.llm,{})).then(function(body){var value=rpcValue(body);setCatalog({status:value&&Array.isArray(value.groups)?'ready':'error',groups:value&&Array.isArray(value.groups)?value.groups:[]});},function(){setCatalog({status:'error',groups:[]});});
        }catch(_){setCatalog({status:'error',groups:[]});return Promise.resolve();}
      }

      React.useEffect(function(){var disposed=false;var stops=[];function refresh(){if(!disposed)void loadCatalog();}refresh();try{if(props&&props.remote&&typeof props.remote.$on==='function'){stops.push(props.remote.$on('llm/adapters-updated',refresh));stops.push(props.remote.$on('settings/document-updated',refresh));}}catch(_){}try{if(props&&typeof props.subscribeConnectionReset==='function')stops.push(props.subscribeConnectionReset(refresh));}catch(_){}return function(){disposed=true;stops.forEach(function(stop){try{if(typeof stop==='function')stop();}catch(_){}});};},[props&&props.getConnection,props&&props.remote,props&&props.subscribeConnectionReset]);

      if(!snapshot||snapshot.status!=='ready')return null;
      var saved=obj(snapshot.value),writable=snapshot.writable===true,saving=saveState.status==='saving',local=localPage(),h=React.createElement;
      var groups=availableGroups(catalog.groups,snapshot),groupById=new Map(groups.map(function(group){return [group.id,group];}));
      function hasDraft(key){return Object.prototype.hasOwnProperty.call(drafts,key);}
      function value(key,fallback){var out=hasDraft(key)?drafts[key]:saved[key];return out===undefined?fallback:out;}
      function setValue(key,next){setDrafts(function(previous){var copy=Object.assign({},previous);copy[key]=next;return copy;});setSaveState({status:'idle'});}
      function chainRows(){return chainDraft===undefined?chainUiRows(saved.providers):chainDraft;}
      function wrapperRows(){return wrapperDraft===undefined?expandWrappers(saved.wrappedProviders):wrapperDraft;}
      function guidanceRows(){var base=arr(saved.guidanceOverrides);return guidanceDraft===undefined?(base.length?base:[{kind:'',text:''}]):guidanceDraft;}
      var chainCanonical=canonicalChain(saved.providers,chainRows()),chainDirty=chainDraft!==undefined&&!same(chainCanonical,arr(saved.providers));
      var wrapperCanonical=collapseWrappers(wrapperRows()),wrapperDirty=wrapperDraft!==undefined&&!same(wrapperCanonical,arr(saved.wrappedProviders));
      var guidanceCanonical=canonicalGuidance(guidanceRows()),guidanceDirty=guidanceDraft!==undefined&&!same(guidanceCanonical,arr(saved.guidanceOverrides));
      var dirtyKeys=Object.keys(drafts).filter(function(key){return !same(drafts[key],saved[key]);});
      var dirty=dirtyKeys.length>0||chainDirty||wrapperDirty||guidanceDirty;
      var valid=validChain(chainRows())&&validWrapperRows(wrapperRows())&&validGuidance(guidanceRows());
      async function save(){if(!scope||typeof scope.set!=='function'||!writable||saving||!dirty||!valid)return;setSaveState({status:'saving'});try{var writes=[];if(chainDirty)writes.push(['providers',chainCanonical]);if(wrapperDirty)writes.push(['wrappedProviders',wrapperCanonical]);if(guidanceDirty)writes.push(['guidanceOverrides',guidanceCanonical]);dirtyKeys.forEach(function(key){writes.push([key,drafts[key]]);});for(var i=0;i<writes.length;i+=1){await scope.set(writes[i][0],writes[i][1]);if(typeof scope.load==='function')await scope.load();}setDrafts({});setChainDraft(undefined);setWrapperDraft(undefined);setGuidanceDraft(undefined);setSaveState({status:'saved'});}catch(error){setSaveState({status:'error',error:error&&error.message?error.message:String(error)});}}
      function discard(){setDrafts({});setChainDraft(undefined);setWrapperDraft(undefined);setGuidanceDraft(undefined);setSaveState({status:'idle'});}
      function title(text,help){return h('div',{className:'vr-ia-section-head'},h('h3',{className:'vr-ia-title'},text),help?h('p',{className:'vr-hint'},help):null);}
      function card(children){return h('div',{className:'vr-card vr-card-open vr-ia-card'},h('div',{className:'vr-body',style:{borderTop:0}},children));}
      function toggle(key,label,hint,disabled){return h('label',{className:'vr-ia-toggle-row'},h('div',{className:'vr-ia-grow'},h('span',{className:'vr-label'},label),hint?h('p',{className:'vr-hint'},hint):null),h('input',{type:'checkbox',checked:value(key,false)===true,disabled:disabled||!writable||saving,onChange:function(event){setValue(key,event.target.checked);}}));}
      function numberField(key,label,hint,min,max,disabled){var current=value(key,'');return h('div',{className:'vr-field'},h('span',{className:'vr-label'},label),h('input',{className:'vr-input',type:'number',min:min,max:max,value:current,disabled:disabled||!writable||saving,onChange:function(event){var text=event.target.value;setValue(key,text===''?'':Number(text));}}),hint?h('p',{className:'vr-hint'},hint):null);}
      function textField(key,label,hint,disabled){return h('div',{className:'vr-field'},h('span',{className:'vr-label'},label),h('input',{className:'vr-input',value:String(value(key,'')||''),disabled:disabled||!writable||saving,onChange:function(event){setValue(key,event.target.value);}}),hint?h('p',{className:'vr-hint'},hint):null);}
      function textareaArray(key,label,hint,disabled){return h('div',{className:'vr-field'},h('span',{className:'vr-label'},label),h('textarea',{className:'vr-input vr-ia-textarea',value:arr(value(key,[])).join('\n'),disabled:disabled||!writable||saving,onChange:function(event){setValue(key,event.target.value.split(/\r?\n/).map(function(x){return x.trim();}).filter(Boolean));}}),hint?h('p',{className:'vr-hint'},hint):null);}
      function providerOptions(current){var nodes=[h('option',{value:'',key:'__empty'},tx('选择供应商…','Choose provider…'))];if(current&&!groupById.has(current))nodes.push(h('option',{value:current,key:'__stale-'+current},current+' '+tx('（已保存）','(saved)')));groups.forEach(function(group){nodes.push(h('option',{value:group.id,key:group.id},group.name&&group.name!==group.id?group.name+' ('+group.id+')':group.id));});return nodes;}
      function modelOptions(provider,current,allowAll){var group=groupById.get(provider),models=arr(group&&group.models),listed=new Set(models.map(function(model){return model&&model.id;}).filter(Boolean)),nodes=[h('option',{value:'',key:'__empty'},allowAll?tx('全部模型','All models'):(provider?tx('选择模型…','Choose model…'):tx('先选供应商','Choose provider first')))];if(current&&!listed.has(current))nodes.push(h('option',{value:current,key:'__stale-'+current},current+' '+tx('（已保存）','(saved)')));models.forEach(function(model){if(model&&typeof model.id==='string'&&model.id)nodes.push(h('option',{value:model.id,key:model.id},modelName(group,model.id)));});return nodes;}
      function modelRow(row,index,rows,setRows,allowAll){var ready=groups.length>0;function update(next){var copy=rows.map(function(item){return Object.assign({},item);});copy[index]=next;setRows(copy);setSaveState({status:'idle'});}function remove(){var copy=rows.filter(function(_,i){return i!==index;});setRows(copy.length?copy:[{provider:'',model:'',fallbacks:[]}]);setSaveState({status:'idle'});}return h('div',{className:'vr-chain-row',key:index},ready?h('select',{className:'vr-input vr-select',value:row.provider||'',disabled:!writable||saving,onChange:function(event){update(Object.assign({},row,{provider:event.target.value,model:''}));}},providerOptions(row.provider||'')):h('input',{className:'vr-input',placeholder:'provider',value:row.provider||'',disabled:!writable||saving,onChange:function(event){update(Object.assign({},row,{provider:event.target.value}));}}),ready?h('select',{className:'vr-input vr-select',value:row.model||'',disabled:!writable||saving||!row.provider,onChange:function(event){update(Object.assign({},row,{model:event.target.value}));}},modelOptions(row.provider||'',row.model||'',allowAll)):h('input',{className:'vr-input',placeholder:allowAll?tx('model（留空=全部）','model (empty = all)'):'model',value:row.model||'',disabled:!writable||saving,onChange:function(event){update(Object.assign({},row,{model:event.target.value}));}}),h('button',{type:'button',className:'vr-reset',disabled:!writable||saving,onClick:remove},tx('移除','Remove')));}

      function generalPage(){var rows=chainRows(),configured=rows.filter(function(row){return row&&row.provider&&row.model;}),fallback=value('freeFallback',true)!==false,status=configured.length?tx('识图已就绪','Vision ready'):fallback?tx('已可使用内置免费识图','Built-in free vision is ready'):tx('暂无可用识图模型','No vision model configured'),ordered=configured.map(function(row){return row.model;});if(fallback)ordered.push(tx('内置免费兜底','Built-in free fallback'));return h(React.Fragment,null,title(tx('常规','General'),tx('这里只设置“谁负责看图”。聊天模型仍在聊天页选择。','This page only decides who reads images; the chat model is still chosen in chat.')),card([h('div',{className:'vr-ia-status',key:'status'},h('strong',null,(configured.length||fallback?'● ':'⚠ ')+status),h('p',{className:'vr-hint'},tx('需要看图时，在聊天框旁点击「👁 识图」。','Use “👁 Vision” beside the chat box when an image needs inspection.')),ordered.length?h('p',{className:'vr-ia-chain-summary'},ordered.join(' → ')):null)]),card([h('div',{className:'vr-field',id:'vr-vision-backend-chain','data-vr-guide-target':'vision-backend',key:'chain'},h('span',{className:'vr-label'},tx('识图模型','Vision models')),h('p',{className:'vr-hint'},tx('从上到下依次尝试；前一个失败时自动切换到下一个。','Models are tried from top to bottom and fall through automatically on failure.')),rows.map(function(row,index){return modelRow(row,index,rows,setChainDraft,false);}),h('button',{type:'button',className:'vr-btn',disabled:!writable||saving,onClick:function(){setChainDraft(rows.concat([{provider:'',model:'',fallbacks:[]}]))}},tx('+ 添加备用识图模型','+ Add fallback vision model'))),toggle('freeFallback',tx('内置免费兜底','Built-in free fallback'),tx('前面的模型都失败后自动尝试，无需 API Key。','Tried after your models fail; no API key required.'))]));}
      function strategyPage(){var structured=value('structuredVisionBootstrap',false)===true,depth=String(value('visionDepth','standard')||'standard'),rows=guidanceRows();return h(React.Fragment,null,title(tx('识图策略','Vision strategy'),tx('只有想改变识图方式或质量时才需要来这里。','Change these only when you want different vision behavior or quality.')),card([toggle('tool',tx('Agent 按需使用识图工具','Let the agent use vision tools on demand'),tx('允许聊天模型根据任务继续细看、OCR、定位、裁剪和比较图片。建议开启。','Lets the chat model inspect, OCR, locate, crop, and compare images as needed. Recommended on.')),toggle('structuredVisionBootstrap',tx('结构化预识别（1+x）','Structured pre-scan (1+x)'),tx('先获取一次全局视觉基线，再针对当前任务继续取证。','Gets a global visual baseline first, then gathers task-specific evidence.'))]),structured?card([h('div',{className:'vr-field',key:'depth'},h('span',{className:'vr-label'},tx('看图深度','Vision depth')),h('select',{className:'vr-input vr-select',value:depth,disabled:!writable||saving,onChange:function(event){setValue('visionDepth',event.target.value);}},h('option',{value:'fast'},tx('快速','Fast')),h('option',{value:'standard'},tx('标准','Standard')),h('option',{value:'deep'},tx('深度','Deep')),h('option',{value:'custom'},tx('自定义','Custom')))),depth==='custom'?numberField('visionDepthMaxCalls',tx('最多追加识图调用','Maximum additional vision calls'),null,0,100):null,h('div',{className:'vr-field',key:'guidance'},h('span',{className:'vr-label'},tx('自定义识图引导','Custom vision guidance')),h('p',{className:'vr-hint'},tx('通常无需设置。','Usually unnecessary.')),rows.map(function(row,index){return h('div',{className:'vr-chain-row',key:index},h('input',{className:'vr-input',placeholder:'document',value:row.kind||'',disabled:!writable||saving,onChange:function(event){var copy=rows.map(function(item){return Object.assign({},item);});copy[index]={kind:event.target.value,text:row.text||''};setGuidanceDraft(copy);}}),h('input',{className:'vr-input',placeholder:tx('重点关注合同条款和签名','Focus on clauses and signatures'),value:row.text||'',disabled:!writable||saving,onChange:function(event){var copy=rows.map(function(item){return Object.assign({},item);});copy[index]={kind:row.kind||'',text:event.target.value};setGuidanceDraft(copy);}}),h('button',{type:'button',className:'vr-reset',disabled:!writable||saving,onClick:function(){var copy=rows.filter(function(_,i){return i!==index;});setGuidanceDraft(copy.length?copy:[{kind:'',text:''}]);}},tx('移除','Remove')));}),h('button',{type:'button',className:'vr-btn',disabled:!writable||saving,onClick:function(){setGuidanceDraft(rows.concat([{kind:'',text:''}]))}},tx('+ 添加自定义引导','+ Add custom guidance')))]) : null);}
      function numberFieldLocal(key,current,update,min,max){return h('div',{className:'vr-field'},h('span',{className:'vr-label'},key),h('input',{className:'vr-input',type:'number',step:'0.1',min:min,max:max,value:current[key]===undefined?'':current[key],disabled:!writable||saving,onChange:function(event){var patch={};patch[key]=event.target.value===''?undefined:Number(event.target.value);update(patch);}}));}
      function localProviderCard(key,label,defaults,which){var current=Object.assign({},defaults,obj(value(key,{}))),opened=which==='ollama'?open.ollama:open.lmstudio;function update(patch){setValue(key,Object.assign({},current,patch));}return card([h('div',{className:'vr-ia-backend-head',key:'head'},h('div',null,h('strong',null,label),h('p',{className:'vr-hint'},current.enabled?tx('已启用','Enabled'):tx('未启用','Disabled'))),h('label',null,h('input',{type:'checkbox',checked:current.enabled===true,disabled:!writable||saving,onChange:function(event){update({enabled:event.target.checked});}}))),h('div',{className:'vr-field',key:'model'},h('span',{className:'vr-label'},tx('模型','Model')),h('input',{className:'vr-input',value:String(current.model||''),disabled:!writable||saving,onChange:function(event){update({model:event.target.value});}})),h('div',{className:'vr-field',key:'url'},h('span',{className:'vr-label'},tx('服务地址','Service URL')),h('input',{className:'vr-input',value:String(current.baseURL||''),disabled:!writable||saving,onChange:function(event){update({baseURL:event.target.value});}})),h('button',{type:'button',className:'vr-btn',onClick:function(){setOpen(Object.assign({},open,which==='ollama'?{ollama:!opened}:{lmstudio:!opened}));}},opened?tx('收起高级连接设置','Hide advanced connection settings'):tx('高级连接设置','Advanced connection settings')),opened?h('div',{className:'vr-ia-subgrid',key:'advanced'},h('div',{className:'vr-field'},h('span',{className:'vr-label'},tx('请求协议','Request protocol')),h('select',{className:'vr-input',value:current.format==='anthropic'?'anthropic':'openai',disabled:!writable||saving,onChange:function(event){update({format:event.target.value});}},h('option',{value:'openai'},'OpenAI'),h('option',{value:'anthropic'},'Anthropic'))),numberFieldLocal('temperature',current,update,0,2),numberFieldLocal('top_p',current,update,0,1)):null]);}
      function localPageContent(){if(!local)return h(React.Fragment,null,title(tx('本地与设备','Local & device')),card([h('p',{className:'vr-hint',key:'remote'},tx('本地视觉后端和桌面截图只能在运行 DSH 的机器上配置。','Local vision backends and desktop capture can only be configured on the DSH machine.'))]));return h(React.Fragment,null,title(tx('本地与设备','Local & device'),tx('本地运行视觉模型可减少 API 费用和图片上传。','Run vision locally to reduce API cost and image uploads.')),localProviderCard('localOllama','Ollama',{enabled:false,baseURL:'http://127.0.0.1:11434/v1',model:'qwen2.5vl',format:'openai'},'ollama'),localProviderCard('localLmStudio','LM Studio',{enabled:false,baseURL:'http://localhost:1234/v1',model:'',format:'openai'},'lmstudio'),card([toggle('desktopScreenshot',tx('允许 Agent 读取桌面截图','Allow the agent to capture the desktop'),tx('这是独立的隐私权限；只有需要主动查看桌面时才开启。','This is a separate privacy permission; enable only when desktop inspection is needed.'))]));}
      function wrappersEditor(){var rows=wrapperRows();return h('div',{className:'vr-field'},h('span',{className:'vr-label'},tx('哪些聊天模型可以开启识图','Which chat models can use Vision mode')),h('p',{className:'vr-hint'},tx('通常无需修改；模型留空表示整个 Provider。','Usually leave this alone; an empty model means the whole provider.')),rows.map(function(row,index){return modelRow(row,index,rows,setWrapperDraft,true);}),h('button',{type:'button',className:'vr-btn',disabled:!writable||saving,onClick:function(){setWrapperDraft(rows.concat([{provider:'',model:''}]))}},tx('+ 添加范围','+ Add scope')));}
      function textProviderEditor(){var current=Object.assign({provider:'',model:''},obj(value('textProvider',{}))),ready=groups.length>0;return h('div',{className:'vr-field'},h('span',{className:'vr-label'},tx('文字回退模型','Text fallback model')),h('div',{className:'vr-chain-row'},ready?h('select',{className:'vr-input',value:current.provider||'',disabled:!writable||saving,onChange:function(event){setValue('textProvider',{provider:event.target.value,model:''});}},providerOptions(current.provider||'')):h('input',{className:'vr-input',value:current.provider||'',onChange:function(event){setValue('textProvider',{provider:event.target.value,model:current.model||''});}}),ready?h('select',{className:'vr-input',value:current.model||'',disabled:!writable||saving||!current.provider,onChange:function(event){setValue('textProvider',{provider:current.provider||'',model:event.target.value});}},modelOptions(current.provider||'',current.model||'',false)):h('input',{className:'vr-input',value:current.model||'',onChange:function(event){setValue('textProvider',{provider:current.provider||'',model:event.target.value});}})));}
      function advancedPage(){var routing=value('routing',false)===true,turnBudget=Number(value('visionTurnBudgetMs',0))||0,customBudget=turnBudget>0;return h(React.Fragment,null,title(tx('高级','Advanced'),tx('这些默认值对大多数用户已经合适。','Defaults are suitable for most users.')),card([h('h4',{className:'vr-ia-subtitle',key:'p'},tx('性能与稳定性','Performance & stability')),toggle('downscale',tx('自动缩放','Auto downscale')),numberField('downscaleMaxPixels',tx('图片像素上限','Image pixel limit'),null,1000,100000000),toggle('cache',tx('识图缓存','Vision answer cache')),numberField('cacheTtlSeconds',tx('缓存有效期（秒）','Cache TTL (seconds)'),null,0,31536000),numberField('cacheMaxEntries',tx('最大缓存数量','Maximum cached answers'),null,1,100000),h('h4',{className:'vr-ia-subtitle',key:'t'},tx('超时','Timeouts')),numberField('timeoutMs',tx('单次模型请求','Single model request'),tx('毫秒。','Milliseconds.'),1000,600000),numberField('visionTaskTimeoutMs',tx('单个视觉任务','Single visual task'),null,1000,180000),numberField('ocrTimeoutMs',tx('OCR 任务','OCR task'),null,1000,120000),h('div',{className:'vr-field',key:'budget'},h('span',{className:'vr-label'},tx('整轮视觉工具上限','Whole-turn vision-tool limit')),h('select',{className:'vr-input',value:customBudget?'custom':'unlimited',disabled:!writable||saving,onChange:function(event){setValue('visionTurnBudgetMs',event.target.value==='unlimited'?0:(turnBudget>0?turnBudget:180000));}},h('option',{value:'unlimited'},tx('不限制（推荐）','Unlimited (recommended)')),h('option',{value:'custom'},tx('自定义','Custom'))),customBudget?numberField('visionTurnBudgetMs',tx('上限（毫秒）','Limit (ms)'),null,10000,600000):null)]),card([h('h4',{className:'vr-ia-subtitle',key:'cost'},tx('模型顺序与成本','Model order & cost')),toggle('freeCloudFirst',tx('免费云模型优先','Try free cloud models first'))]),card([h('h4',{className:'vr-ia-subtitle',key:'scope'},tx('识图模式范围','Vision mode scope')),toggle('autoWrapProviders',tx('自动允许已启用模型使用识图','Automatically allow enabled models to use Vision mode')),wrappersEditor()]),local?card([h('h4',{className:'vr-ia-subtitle',key:'network'},tx('网络与远程','Network & remote')),toggle('allowRemoteSettings',tx('允许可信 Host 远程修改设置','Allow trusted-host remote settings')),textField('proxy',tx('代理地址','Proxy URL'),tx('留空关闭代理。','Leave empty to disable.')),textareaArray('proxyHosts',tx('走代理的域名','Proxied hosts'),tx('每行一个。','One per line.'))]):null,card([h('h4',{className:'vr-ia-subtitle',key:'compat'},tx('兼容模式','Compatibility')),toggle('rewriteImages',tx('保护纯文本模型','Protect text-only models')),toggle('routing',tx('整轮视觉路由（旧工作流）','Whole-turn vision routing (legacy workflow)')),routing?toggle('reverseRouting',tx('纯文字消息继续使用聊天模型','Keep text-only messages on the chat model')):null,routing?textProviderEditor():null]),card([h('button',{type:'button',className:'vr-btn',key:'dev',onClick:function(){setOpen(Object.assign({},open,{developer:!open.developer}));}},open.developer?tx('隐藏开发者设置','Hide developer settings'):tx('显示开发者设置','Show developer settings')),open.developer?h('div',{className:'vr-ia-dev',key:'body'},toggle('progressiveTools',tx('渐进式工具暴露','Progressive tool exposure')),local?toggle('stealth','Stealth'):null,local?textField('wrapperRoute',tx('包装路由名','Wrapper route name')):null,local?textField('chainRoute',tx('视觉链路由名','Vision chain route name')):null,textareaArray('extraVisionModels',tx('额外视觉能力标记','Extra vision capability labels'))):null]));}
      function diagnosticsPage(){var providerCount=groups.length,modelCount=groups.reduce(function(total,group){return total+arr(group&&group.models).length;},0),configured=chainRows().filter(function(row){return row&&row.provider&&row.model;}).length,ollama=obj(value('localOllama',{})),lm=obj(value('localLmStudio',{}));var rows=[[tx('设置协议','Settings contract'),String(value('settingsContractRevision','—'))],[tx('模型目录','Model catalog'),catalog.status==='ready'?tx('正常','Ready'):catalog.status==='loading'?tx('检测中','Checking'):tx('不可用','Unavailable')],[tx('可选 Provider','Selectable providers'),String(providerCount)],[tx('可选模型','Selectable models'),String(modelCount)],[tx('已配置识图模型','Configured vision models'),String(configured)],[tx('内置免费兜底','Built-in free fallback'),value('freeFallback',true)!==false?tx('已启用','Enabled'):tx('已关闭','Disabled')],[tx('Ollama','Ollama'),ollama.enabled===true?tx('已启用','Enabled'):tx('未启用','Disabled')],[tx('LM Studio','LM Studio'),lm.enabled===true?tx('已启用','Enabled'):tx('未启用','Disabled')],[tx('桌面截图','Desktop capture'),value('desktopScreenshot',false)===true?tx('已启用','Enabled'):tx('未启用','Disabled')],[tx('代理','Proxy'),String(value('proxy','')).trim()?tx('已配置','Configured'):tx('未配置','Not configured')],[tx('远程设置','Remote settings'),value('allowRemoteSettings',false)===true?tx('已启用','Enabled'):tx('未启用','Disabled')]];function reportText(){return ['Vision Router diagnostics'].concat(rows.map(function(row){return row[0]+': '+row[1];})).join('\n');}async function copyReport(){try{if(typeof navigator!=='undefined'&&navigator.clipboard&&typeof navigator.clipboard.writeText==='function')await navigator.clipboard.writeText(reportText());setSaveState({status:'copied'});}catch(error){setSaveState({status:'error',error:error&&error.message?error.message:String(error)});}}return h(React.Fragment,null,title(tx('诊断','Diagnostics'),tx('这里只展示当前状态，不修改设置。','This page reports current state and does not change settings.')),card(rows.map(function(row,index){return h('div',{className:'vr-ia-diag-row',key:index},h('span',null,row[0]),h('strong',null,row[1]));})),card([h('div',{className:'vr-ia-actions',key:'actions'},h('button',{type:'button',className:'vr-btn',onClick:function(){void loadCatalog();}},tx('重新检测模型','Re-detect models')),h('button',{type:'button',className:'vr-btn',onClick:function(){void copyReport();}},tx('复制诊断信息','Copy diagnostics'))),h('p',{className:'vr-hint',key:'doctor'},tx('需要完整环境诊断时，在运行 DSH 的机器上执行：dsh-vision-router doctor','For a full environment report, run on the DSH machine: dsh-vision-router doctor'))]));}

      var pages=[['general',tx('常规','General')],['strategy',tx('识图策略','Vision strategy')],['local',tx('本地与设备','Local & device')],['advanced',tx('高级','Advanced')],['diagnostics',tx('诊断','Diagnostics')]];
      var content=page==='strategy'?strategyPage():page==='local'?localPageContent():page==='advanced'?advancedPage():page==='diagnostics'?diagnosticsPage():generalPage();
      return h('div',{className:'vr-settings-ia-root','data-vr-settings-ia':'1'},h('div',{className:'vr-ia-shell'},h('nav',{className:'vr-ia-nav','aria-label':tx('Vision Router 设置分类','Vision Router settings categories')},pages.map(function(item){return h('button',{type:'button',key:item[0],className:'vr-ia-nav-item'+(page===item[0]?' vr-ia-nav-active':''),onClick:function(){setPage(item[0]);}},item[1]);})),h('main',{className:'vr-ia-main'},content)),dirty?h('div',{className:'vr-ia-savebar'},h('span',{className:valid?'vr-hint':'vr-failed'},valid?tx('有未保存的修改','Unsaved changes'):tx('请先补全未填写的配置行','Complete unfinished configuration rows first')),h('div',{className:'vr-ia-save-actions'},h('button',{type:'button',className:'vr-btn',disabled:saving,onClick:discard},tx('放弃','Discard')),h('button',{type:'button',className:'vr-btn vr-btn-save',disabled:!valid||!writable||saving,onClick:function(){void save();}},saving?tx('保存中…','Saving…'):tx('保存','Save')))):saveState.status==='saved'?h('div',{className:'vr-ia-toast'},tx('已保存','Saved')):saveState.status==='copied'?h('div',{className:'vr-ia-toast'},tx('诊断信息已复制','Diagnostics copied')):saveState.status==='error'?h('div',{className:'vr-ia-toast vr-failed'},tx('操作失败：','Action failed: ')+String(saveState.error||'unknown')):null);
    };
  }

  function wrapSlots(slots,React){if(!slots||(typeof slots!=='object'&&typeof slots!=='function'))return slots;var IA=makeComponent(React);return new Proxy(slots,{get:function(target,property){if(property==='register'){var register=Reflect.get(target,property,target);if(typeof register!=='function')return register;return function(options,component){var args=Array.prototype.slice.call(arguments);if(options&&options.name==='settings.section'&&options.id===SECTION_ID&&component)args[1]=IA;return register.apply(target,args);};}var value=Reflect.get(target,property,target);return typeof value==='function'?value.bind(target):value;}});}
  function wrapContext(ctx,React){if(!ctx||typeof ctx!=='object')return ctx;if(contexts&&contexts.has(ctx))return contexts.get(ctx);var slots=wrapSlots(ctx.slots,React),locale=wrapLocale(ctx.locale);var wrapped=new Proxy(ctx,{get:function(target,property){if(property==='slots')return slots;if(property==='locale')return locale;var value=Reflect.get(target,property,target);return typeof value==='function'?value.bind(target):value;}});if(contexts)contexts.set(ctx,wrapped);return wrapped;}
  function patchLoader(loader){if(!loader||(typeof loader!=='object'&&typeof loader!=='function'))return;if(typeof loader.load==='function'&&!loader.load.__visionRouterSettingsIA){var original=loader.load;function load(spec){if(spec&&spec.id===TARGET&&typeof spec.factory==='function'){var factory=spec.factory;spec=Object.assign({},spec,{factory:function(require){var exports=factory(require),React;try{React=require('react');}catch(_){React=undefined;}if(React&&exports&&typeof exports.apply==='function'&&!exports.apply.__visionRouterSettingsIA){var apply=exports.apply;var wrappedApply=function(ctx){var rest=Array.prototype.slice.call(arguments,1);return apply.apply(exports,[wrapContext(ctx,React)].concat(rest));};Object.defineProperty(wrappedApply,'__visionRouterSettingsIA',{value:true});exports.apply=wrappedApply;}return exports;}});}return original.call(loader,spec);}Object.defineProperty(load,'__visionRouterSettingsIA',{value:true});loader.load=load;}}
  function install(){if(window.__ModuleLoader__){patchLoader(window.__ModuleLoader__);return;}var descriptor=Object.getOwnPropertyDescriptor(window,'__ModuleLoader__');if(descriptor&&descriptor.configurable===false)return;var stored;Object.defineProperty(window,'__ModuleLoader__',{configurable:true,enumerable:true,get:function(){return stored;},set:function(value){stored=value;patchLoader(value);Object.defineProperty(window,'__ModuleLoader__',{configurable:true,enumerable:true,writable:true,value:stored});}});}
  try{install();}catch(_){}
})();`

const SETTINGS_IA_STYLE = String.raw`
.vr-settings-ia-root{width:100%;min-width:0}.vr-ia-shell{display:grid;grid-template-columns:158px minmax(0,1fr);gap:18px;align-items:start}.vr-ia-nav{position:sticky;top:12px;display:flex;flex-direction:column;gap:4px;padding:4px}.vr-ia-nav-item{border:0;background:transparent;color:inherit;text-align:left;padding:9px 11px;border-radius:9px;font:inherit;cursor:pointer;opacity:.72}.vr-ia-nav-active{background:color-mix(in srgb,currentColor 10%,transparent);font-weight:650;opacity:1}.vr-ia-main{min-width:0}.vr-ia-section-head{margin:0 0 12px}.vr-ia-title{margin:0 0 4px;font-size:18px}.vr-ia-card{margin-bottom:12px}.vr-ia-toggle-row,.vr-ia-backend-head,.vr-ia-diag-row{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;padding:10px 0}.vr-ia-diag-row+.vr-ia-diag-row,.vr-ia-toggle-row+.vr-ia-toggle-row{border-top:1px solid color-mix(in srgb,currentColor 10%,transparent)}.vr-ia-grow{min-width:0;flex:1}.vr-ia-status strong{font-size:15px}.vr-ia-chain-summary{margin:8px 0 0;font-size:12px;opacity:.74}.vr-ia-subtitle{margin:12px 0 8px;font-size:14px}.vr-ia-subgrid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.vr-ia-textarea{min-height:88px;resize:vertical}.vr-ia-savebar{position:sticky;bottom:10px;z-index:5;display:flex;align-items:center;justify-content:space-between;gap:12px;margin-top:14px;padding:10px 12px;border:1px solid color-mix(in srgb,currentColor 14%,transparent);border-radius:11px;background:color-mix(in srgb,Canvas 94%,transparent);backdrop-filter:blur(10px)}.vr-ia-save-actions,.vr-ia-actions{display:flex;gap:8px;flex-wrap:wrap}.vr-ia-toast{margin-top:10px;font-size:12px;opacity:.75}.vr-ia-dev{margin-top:12px}.vr-settings-ia-root .vr-chain-row{align-items:center}.vr-settings-ia-root .vr-field{margin-bottom:10px}
@media (max-width:760px){.vr-ia-shell{grid-template-columns:1fr;gap:10px}.vr-ia-nav{position:static;flex-direction:row;overflow:auto;padding:0 0 4px}.vr-ia-nav-item{white-space:nowrap}.vr-ia-subgrid{grid-template-columns:1fr}.vr-ia-savebar{bottom:6px}}
`

export function injectSettingsIaClientPrelude(html) {
  if (typeof html !== 'string' || html.includes(CLIENT_MARK)) return html
  const script = `<style ${CLIENT_MARK}>${SETTINGS_IA_STYLE}</style><script ${CLIENT_MARK}>${SETTINGS_IA_CLIENT_PRELUDE.replace(/<\/script/gi, '<\\/script')}</script>`
  const closeHead = html.indexOf('</head>')
  return closeHead === -1 ? `${html}${script}` : `${html.slice(0, closeHead)}${script}${html.slice(closeHead)}`
}

export function installSettingsIaClientPrelude(ctx) {
  ctx?.inject?.(['webServer'], (webCtx) => {
    webCtx.effect(
      () => webCtx.webServer.tapIndex(injectSettingsIaClientPrelude),
      'vision-router: settings information architecture client prelude',
    )
  })
}
