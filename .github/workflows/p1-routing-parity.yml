name: P1 Routing Parity

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  parity:
    name: routing-parity (${{ matrix.node }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node: ['22', '24']
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6
        with:
          version: 11.20.0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: ${{ matrix.node }}
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: P1 routing, authority, runtime and execution-order parity
        run: >-
          node --test
          tests/vision-routing-evidence-parity.test.js
          tests/vision-execution-order.test.js
          tests/vision-execution-order-plan-parity.test.js
          tests/vision-execution-order-apply.test.js
          tests/vision-execution-order-core-wiring.test.js
          tests/vision-routing-runtime-parity.test.js
          tests/vision-capability-router.test.js
          tests/vision-routing-product.test.js
          tests/vision-runtime-performance.test.js
